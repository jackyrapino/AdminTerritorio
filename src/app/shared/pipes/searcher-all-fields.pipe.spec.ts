import { SearcherAllFieldsPipe } from './searcher-all-fields.pipe';

describe('SearcherAllFieldsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearcherAllFieldsPipe();
    expect(pipe).toBeTruthy();
  });
});
